{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build (CMake/Ninja)",
      "type": "shell",
      "command": "cmake",
      "args": [
        "--build",
        "build",
        "-j"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": [],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    },
    {
      "label": "Flash (OpenOCD, HEX)",
      "type": "shell",
      "command": "openocd",
      "args": [
        "-f",
        "interface/stlink.cfg",
        "-f",
        "target/stm32f4x.cfg",
        "-c",
        "transport select swd; adapter speed 950; init; reset halt; flash write_image erase build/CNCv1.hex; verify_image build/CNCv1.hex; reset run; exit"
      ],
      "dependsOn": "Build (CMake/Ninja)",
      "problemMatcher": []
    }
  ]
}
